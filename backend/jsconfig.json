{
  "compilerOptions": {
    "baseUrl": "./" // Set the base directory for resolving module names
  },
  "include": ["**/*"], // Include all files in the backend folder
  "exclude": ["node_modules"] // Exclude the node_modules folder
}